interface:
  display_name: "AIRD Discovery Loop"
  short_description: "Create linted PRD/TRD/DoD and delivery workorders"
  default_prompt: "Use $aird-discovery-loop to turn this feature idea into an AIRD package."

policy:
  allow_implicit_invocation: false
