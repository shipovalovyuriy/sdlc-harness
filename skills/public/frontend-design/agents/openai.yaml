interface:
  display_name: "Frontend Design"
  short_description: "Design and visually verify polished web UI"
  default_prompt: "Use $frontend-design to design and implement a production-grade interface, inspect the relevant positive visual exemplars, treat red-X frames only as failures, and verify the rendered result on desktop and mobile."
